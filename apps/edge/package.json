{
  "name": "@convertio/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && node --test \"dist/test/**/*.test.js\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@convertio/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
